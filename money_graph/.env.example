# Optional: copy to money_graph/.env only if it does not already exist.
# Add the key locally. Native: restart the server.
# Docker: docker compose up -d --force-recreate app
# Local tools work with an empty key. Never commit this file with a real key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
